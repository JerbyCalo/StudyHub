# Logo Placeholder

Replace this file with the actual Baylo logo:

- Filename: `logo.png` (also add `logo.svg` if available)
- Recommended size: 512×512px minimum
- Place in: `/public/logo.png`
- Then update `BayloLogo.jsx` to use:

```jsx
<Image src="/logo.png" alt="Baylo" width={32} height={32} />
```
